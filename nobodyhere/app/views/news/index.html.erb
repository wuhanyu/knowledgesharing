<h1>求职信息<small>（每小时更新，覆盖水木、北邮人和饮水思源）</small></h1>
<ul class="pager">
  <li
  <% if @news.count < 50
  	 %> class="disabled" <%
  	 end %>
  ><%= link_to "上一页" , {:controller => "news", :action => "index", :start=> @start + 50 }%></li>
  <li 
  <% if @start <= 0
  	 %> class="disabled" <%
  	 end %>
  ><%= link_to "下一页" , {:controller => "news", :action => "index", :start=> @start - 50 > 0 ? @start - 50 : 0}%></li>
</ul>
<table class="table table-bordered table-striped center-table">
  <tr>
  	<th>
    <th>标题</th>
    <th>浏览数</th>
    <th>获取时间</th>
  </tr>

<% @news.each do |news| %>
  <tr>
  	<td><%
      		if (news.created_at > 1.hour.ago)
      			%><span class="label label-danger">最新</span><%
      		elsif (news.created_at > 1.day.ago)
      			%><span class="label label-warning">今日</span><%
      		end
      	    %></td>
    <td><%= link_to news.title , {:controller => "news", :action => "jump", :id=> news.id}, :target => "_blank"%></a></td>
    <td><%= news.view_number ? news.view_number : 0 %></td>
    <td><%= news.created_at %></td>
  </tr>
<% end %>
</table>
<ul class="pager">
  <li
  <% if @news.count < 50
  	 %> class="disabled" <%
  	 end %>
  ><%= link_to "上一页" , {:controller => "news", :action => "index", :start=> @start + 50 }%></li>
  <li 
  <% if @start <= 0
  	 %> class="disabled" <%
  	 end %>
  ><%= link_to "下一页" , {:controller => "news", :action => "index", :start=> @start - 50 > 0 ? @start - 50 : 0}%></li>
</ul>
<br />
